apiVersion: v1
kind: ConfigMap
metadata:
  name: plancia-config
  namespace: plancia
data:
  URLkeycloak: ${NAVARCOS_KEYCLOAK_URL}
  URLplancia-be: ${NAVARCOS_PLANCIA_BE_URL}
  URLplancia-fe: ${NAVARCOS_PLANCIA_FE_URL}
