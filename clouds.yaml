clouds:
  openstack:
    auth:
      auth_url: ${OS_AUTH_URL_}
      username: ${OS_USERNAME_}
    #   project_id: redacted
      project_id: ${OS_PROJECT_ID_}
      project_name: ${OS_PROJECT_NAME_}
      user_domain_name: ${OS_USER_DOMAIN_NAME_}
    region_name: ${OS_REGION_NAME_}
    interface: ${INTERFACE_OSTACK_}
    identity_api_version: ${OS_IDENTITY_API_VERSION_}